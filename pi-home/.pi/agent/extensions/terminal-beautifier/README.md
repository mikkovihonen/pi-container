# Terminal Beautifier Extension

This extension enhances the terminal output in the pi agent with improved formatting and colorization.

## Features

- Enhanced syntax highlighting for various command outputs.
- Subset of LaTeX symbols rendering as unicode glyphs.
- Only applies the replacements outside markdown code blocks

## Installation

The extension is pre-installed with the pi agent.

## Configuration

Currently, there are no configurable options for this extension.
